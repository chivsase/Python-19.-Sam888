pyinstaller --onefile --hidden-import=inquirer --hidden-import=readchar --copy-metadata readchar .\Sam888.py --icon .\image\TeleSMG_logo_for_Telegram_message_with_a_technology_theme.ico
🔄 Full Scenario Example:
Phone 1 logs in.
Sends Message #1 → Waits 35s (random).
Sends Message #2 → Waits 55s (random).
... keeps going until ...
Sends Message #100 → Phone 1 is done!
Phone 2 logs in automatically.
Sends Message #101 → Waits 40s (random).
... and so on.